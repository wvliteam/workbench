# QA 建议使用的 Skill

> **模板文件（内核默认版）。** 业务 workspace 按自身可用的 Skill 填写下表并按需补充「优先级」；
> 未定义推荐 skill 时保留空表即可。所有 skill 通过 `Skill` 工具调用，须遵守本角色的读写边界与只读约束。

| Skill | 何时使用 | 如何使用 |
| --- | --- | --- |
|  |  |  |

## 优先级

（业务 workspace 按需补充；未定义时留空。）
