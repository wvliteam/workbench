# 工具链与验证协议

- 运行构建或测试前先读取目标仓库的 `go.mod`、`package.json` 或等价工具链声明。
- 不要用本机默认工具链替代仓库声明的版本；版本不满足时明确报告阻塞。
- 门禁命令必须在目标仓库目录执行；多仓库门禁分别 `cd` 到对应目录。
- 通过与否以命令退出码和独立测试证据为准，不以日志颜色、警告数量或 IDE 诊断为准。
- 各语言的项目特定版本矩阵应由具体 workspace 的 references 文档维护。
